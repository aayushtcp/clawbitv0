// Enum for custon heading layout variants
export enum HeadingVariant {
  HORIZONTAL = 'horizontal',
  VERTICAL = 'vertical',
}

// Button Varients
export enum BtnVariant {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  GHOST = 'ghost',
  ANIMATED = 'animated',
}
